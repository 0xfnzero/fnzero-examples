# =========================
# Solana 配置（生产环境）— PumpFun 内盘示例
# =========================

rpc_url: "https://api.mainnet-beta.solana.com"

private_key: ""

nonce_config:
  buy_nonce_accounts:
    - ""
  sell_nonce_accounts:
    - ""

swqos:
  region: "Frankfurt"
  enabled_providers:
    - provider: "Astralane"
      api_token: "YOUR_API_TOKEN_HERE"
      enabled: false
    - provider: "BlockRazor"
      api_token: "YOUR_API_TOKEN_HERE"
      enabled: false
    - provider: "Jito"
      api_token: "YOUR_API_TOKEN_HERE"
      enabled: false
    - provider: "NextBlock"
      api_token: "YOUR_API_TOKEN_HERE"
      enabled: false
    - provider: "Bloxroute"
      api_token: "YOUR_API_TOKEN_HERE"
      enabled: false
    - provider: "ZeroSlot"
      api_token: "YOUR_API_TOKEN_HERE"
      enabled: false
    - provider: "Temporal"
      api_token: "YOUR_API_TOKEN_HERE"
      enabled: false
    - provider: "FlashBlock"
      api_token: "YOUR_API_TOKEN_HERE"
      enabled: false
    - provider: "Node1"
      api_token: "YOUR_API_TOKEN_HERE"
      enabled: false
    - provider: "Default"
      api_token: null
      enabled: false
